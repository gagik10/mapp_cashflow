#Область СлужебныйПрограммныйИнтерфейс

// Выполняет синхронизацию с центральной базой.
//
Процедура Синхронизировать() Экспорт
	
	Если ОбменМобильноеПриложениеВызовСервера.ВходВЦентральнуюБазуВыполнен() Тогда
		ОткрытьФорму("ОбщаяФорма.Синхронизация");
	Иначе
		ОткрытьФорму("ОбщаяФорма.НастройкиСинхронизации");
	КонецЕсли;
	
КонецПроцедуры // Синхронизировать()

#КонецОбласти